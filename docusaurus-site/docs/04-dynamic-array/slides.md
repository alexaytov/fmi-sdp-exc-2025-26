---
title: Динамичен Масив - Свойства и Реализация в C++
theme: white
highlightTheme: github
transition: slide
controls: true
progress: true
slideNumber: true
---

# 📊 Динамичен Масив

## Свойства и Реализация в C++

**Лекция 4** • Структури от Данни и Програмиране

---

## 📋 Съдържание

🎯 **Въведение и Мотивация**

⚡ **Ограничения на Статичните Масиви**

🔄 **Динамични Масиви - Основни Концепции**

💻 **Имплементация в C++**

📈 **Амортизирана Сложност**

🆚 **Сравнение с STL Контейнери**

Note:
Днес ще научите как работят динамичните масиви - фундаментална структура от данни, която комбинира гъвкавостта с ефективността. Ще видите как векторите в C++ работят вътрешно.

---

<!-- .slide: data-background="#e7f3ff" -->

# 🎯 Част 1

## Въведение и Мотивация

---

## 🤔 Защо Динамични Масиви?

### Проблемът със Статичните Масиви

```cpp
int arr[5];  // Фиксиран размер - 5 елемента

// ❌ Какво ако имаме нужда от 6-ти елемент?
// ❌ Какво ако използваме само 2?
// ❌ Не можем да променим размера!
```

--

### Ограничения

- **Фиксиран размер** при декларация
- Трябва да знаем предварително колко елементи ще има
- **Загуба на памет** при прекалено голям масив
- **Невъзможност за разширение** при нужда

Note:
Статичните масиви са ефективни, но негъвкави. В реалния свят рядко знаем точния брой елементи предварително.

---

## Решението: Динамични Масиви

<div class="columns">
<div class="column left">

**✅ Предимства:**
- Променлив размер
- Автоматично разширяване
- Ефективно използване на паметта
- Бърз достъп O(1)

</div>
<div class="column right">

**Примери:**
- `std::vector` в C++
- `ArrayList` в Java
- `list` в Python

</div>
</div>

Note:
Динамичните масиви съчетават най-доброто от двата свята - гъвкавостта и скоростта.

---

<!-- .slide: data-background="#e8f5e9" -->

# Част 2

## Основни Концепции

---

## Size vs Capacity

**Две ключови понятия:**

<div class="columns">
<div class="column left">

### Size (Размер)
- **Брой елементи** в момента
- Променя се при add/remove
- `size ≤ capacity` винаги

```cpp
int getSize() {
    return size;
}
```

</div>
<div class="column right">

### Capacity (Капацитет)
- **Заделена памет**
- Максимум без реалокация
- Обикновено > size

```cpp
int getCapacity() {
    return capacity;
}
```

</div>
</div>

Note:
Разбирането на разликата между size и capacity е критично за изпита!

---

## Защо Capacity > Size?

**Ефективност!**

```
Начално състояние:
capacity = 4, size = 0
[_, _, _, _]

След push_back(10):
capacity = 4, size = 1
[10, _, _, _]

След push_back(20), push_back(30):
capacity = 4, size = 3
[10, 20, 30, _]

Има място за още 1 елемент без преразпределение!
```

Note:
Резервната памет ни позволява да избегнем скъпи операции по копиране при всяко добавяне.

---

## Стратегия за Растеж

**Удвояване на капацитета:**

```cpp
void resize() {
    int new_capacity = capacity * 2;  // Удвояване!

    int* new_data = new int[new_capacity];

    // Копиране на старите данни
    for (int i = 0; i < size; i++) {
        new_data[i] = data[i];
    }

    delete[] data;
    data = new_data;
    capacity = new_capacity;
}
```

Note:
Удвояването е класическата стратегия. Алтернативи включват коефициент 1.5 или добавяне на константа.

---

## Сравнение на Стратегии

| Стратегия | Формула | Амортизирана | Памет |
|-----------|---------|--------------|-------|
| **Удвояване** | `cap * 2` | **O(1)** ✅ | 2x средно |
| Коефициент 1.5 | `cap * 1.5` | **O(1)** ✅ | 1.5x средно |
| +Константа | `cap + 10` | **O(n)** ❌ | По-малко |

Note:
Удвояването осигурява оптимална амортизирана сложност, докато добавянето на константа води до линейна сложност!

---

<!-- .slide: data-background="#fff3e0" -->

# Част 3

## Имплементация в C++

---

## Структура на Класа

```cpp
class DynamicArray {
private:
    int* data;      // Указател към паметта
    int size;       // Брой елементи
    int capacity;   // Заделена памет

    void resize();  // Помощна функция

public:
    DynamicArray(int initial_capacity = 4);
    ~DynamicArray();

    void push_back(int value);
    int get(int index) const;
    int operator[](int index);
};
```

Note:
Три критични член-променливи управляват цялата функционалност.

---

## Конструктор и Деструктор

<div class="columns">
<div class="column left">

**Конструктор:**
```cpp
DynamicArray(int cap = 4)
    : capacity(cap), size(0) {
    data = new int[capacity];
}
```

**Роля:**
- Заделя начална памет
- Инициализира size и capacity

</div>
<div class="column right">

**Деструктор:**
```cpp
~DynamicArray() {
    delete[] data;
    data = nullptr;
}
```

**Роля:**
- Освобождава паметта
- Предотвратява memory leaks
- **КРИТИЧЕН ЗА ИЗПИТА!**

</div>
</div>

Note:
RAII принцип - Resource Acquisition Is Initialization. Ресурси се придобиват в конструктора и освобождават в деструктора.

---

## Метод push_back

```cpp
void push_back(int value) {
    // Проверка за място
    if (size >= capacity) {
        resize();  // Разширяване
    }

    // Добавяне на елемента
    data[size++] = value;
}
```

**Две фази:**
1. **Проверка** - нужна ли е реалокация?
2. **Добавяне** - поставяме елемента

**Сложност:**
- Обикновено: O(1)
- При resize: O(n)
- **Амортизирано: O(1)**

Note:
Това е най-важната операция. Повечето пъти е константна, но понякога отнема линейно време.

---

## Функция resize

```cpp
void resize() {
    int new_capacity = capacity * 2;

    // 1. Заделяне на нова памет
    int* new_data = new int[new_capacity];

    // 2. Копиране на старите данни
    for (int i = 0; i < size; i++) {
        new_data[i] = data[i];
    }

    // 3. Освобождаване на старата памет
    delete[] data;

    // 4. Актуализация
    data = new_data;
    capacity = new_capacity;
}
```

**Време: O(n)** - копираме всички елементи

Note:
Четири стъпки: allocate, copy, free, update. Всички са критични!

---

## Достъп до Елементи

<div class="columns">
<div class="column left">

**Безопасен (get):**
```cpp
int get(int index) const {
    if (index < 0 ||
        index >= size) {
        return -1;  // Грешка
    }
    return data[index];
}
```

**Характеристики:**
- Проверка на границите
- По-безопасен
- Малко по-бавен

</div>
<div class="column right">

**Бърз (operator[]):**
```cpp
int operator[](int index) {
    return data[index];
}
```

**Характеристики:**
- Без проверка
- Максимална скорост
- Познат синтаксис
- Отговорност на програмиста

</div>
</div>

Note:
Двата метода имат различни use cases. Използвайте get() в production за сигурност.

---

## Пример за Използване

```cpp
int main() {
    DynamicArray arr(2);

    cout << "Initial: size=" << arr.getSize()
         << ", capacity=" << arr.getCapacity() << endl;
    // Output: size=0, capacity=2

    arr.push_back(10);
    arr.push_back(20);
    // size=2, capacity=2

    arr.push_back(30);  // Triggers resize!
    // size=3, capacity=4

    cout << "arr[1] = " << arr[1] << endl;
    // Output: arr[1] = 20

    return 0;
}
```

Note:
Третото добавяне предизвиква resize, защото size достига capacity.

---

<!-- .slide: data-background="#f3e5f5" -->

# Част 4

## Амортизирана Сложност

---

## Защо push_back е O(1)?

**Интуиция:** Въпреки че понякога отнема O(n), средно е константно!

**Пример с удвояване:**

```
capacity = 1 → 2 → 4 → 8 → 16 → ... → n

Копирания:  1 + 2 + 4 + 8 + 16 + ... + n
          = 2n - 1 ≈ 2n

За n операции → 2n копирания
Средно: 2n/n = 2 операции на елемент
```

**Резултат: O(1) амортизирано!**

Note:
Това е мощен математически резултат. Скъпите операции стават все по-редки.

---

## Сравнение: Стратегии за Растеж

<div class="columns">
<div class="column left">

**Удвояване (*2):**
```
1 → 2 → 4 → 8 → 16 → 32
Копирания: 1+2+4+8+16 = 31
За 32 елемента = ~2 копия/елемент
✅ O(1) амортизирано
```

</div>
<div class="column right">

**Добавяне (+10):**
```
10 → 20 → 30 → 40 → 50 → 60
Копирания: 10+20+30+40+50 = 150
За 60 елемента = 2.5 копия/елемент
Но расте линейно!
❌ O(n) амортизирано
```

</div>
</div>

Note:
Удвояването осигурява че скъпите операции стават експоненциално по-редки.

---

## Визуализация на Сложност

| Операция | Обикновена | При resize | Амортизирана |
|----------|------------|------------|--------------|
| **push_back** | O(1) | O(n) | **O(1)** ✅ |
| **operator[]** | O(1) | - | **O(1)** ✅ |
| **get** | O(1) | - | **O(1)** ✅ |
| **resize** | - | O(n) | - |

**Заключение:** Динамичните масиви са изключително ефективни!

Note:
Амортизираната O(1) сложност прави динамичните масиви една от най-използваните структури.

---

<!-- .slide: data-background="#e0f2f1" -->

# Част 5

## Сравнение със STL

---

## std::vector - Production Ready

```cpp
#include <vector>
#include <iostream>

int main() {
    std::vector<int> vec;

    vec.push_back(10);
    vec.push_back(20);
    vec.push_back(30);

    std::cout << "Size: " << vec.size() << std::endl;
    std::cout << "Capacity: " << vec.capacity() << std::endl;

    // Безопасен достъп с exception
    try {
        std::cout << vec.at(10) << std::endl;
    } catch (const std::out_of_range& e) {
        std::cout << "Error: " << e.what() << std::endl;
    }

    return 0;
}
```

Note:
std::vector е високо оптимизиран, тестван от години и следва RAII принципа.

---

## Защо std::vector?

<div class="columns">
<div class="column left">

**✅ Предимства:**
- Автоматично управление на памет
- Никакви memory leaks
- Богат интерфейс
- STL алгоритми
- Итератори
- Exception safety
- Тестван и оптимизиран

</div>
<div class="column right">

**Допълнителни методи:**
```cpp
vec.reserve(100);   // Резервира
vec.clear();        // Изчиства
vec.empty();        // Празен ли е?
vec.front();        // Първи елемент
vec.back();         // Последен
vec.insert();       // Вмъква
vec.erase();        // Изтрива
```

</div>
</div>

Note:
В production код ВИНАГИ използвайте std::vector, не имплементирайте собствен!

---

## Кога Собствена Имплементация?

<div class="columns">
<div class="column left">

**Образователни цели:**
- Разбиране на вътрешната структура
- Практика с указатели
- Управление на памет
- Подготовка за интервюта
- ✅ **За този курс!**

</div>
<div class="column right">

**Рядко в production:**
- Специфични изисквания
- Вградени системи
- Специализирана функционалност
- Наследен код
- ⚠️ **Обикновено не е нужно**

</div>
</div>

Note:
Имплементацията е отлично упражнение, но в реалния свят използвайте std::vector.

---

## Статични vs Динамични Масиви

| Характеристика | Статични | Динамични |
|:--------------|:---------|:----------|
| **Размер** | Фиксиран | Променлив |
| **Памет** | Стек | Хийп |
| **Използване** | Константно | Адаптира се |
| **Достъп** | O(1) | O(1) |
| **Добавяне** | Невъзможно | Автоматично |
| **Гъвкавост** | Ниска | Висока |

Note:
Динамичните масиви добавят гъвкавост без да жертват скоростта на достъп.

---

<!-- .slide: data-background="#ffebee" -->

# Част 6

## Често Срещани Грешки

---

## Memory Management

<div class="columns">
<div class="column left">

**❌ ГРЕШНО:**
```cpp
// Грешка 1: delete вместо delete[]
int* arr = new int[5];
delete arr;  // ❌ Undefined behavior!

// Грешка 2: Memory leak
int* arr = new int[5];
arr = new int[10];  // Изтича!

// Грешка 3: Без деструктор
~DynamicArray() { }  // Leak!
```

</div>
<div class="column right">

**✅ ПРАВИЛНО:**
```cpp
// За масиви - delete[]
int* arr = new int[5];
delete[] arr;
arr = nullptr;

// Преди нова алокация - delete
delete[] arr;
arr = new int[10];

// Винаги delete в деструктора
~DynamicArray() {
    delete[] data;
}
```

</div>
</div>

Note:
Тези грешки са изключително чести при начинаещи. Бъдете внимателни!

---

## Логически Грешки

```cpp
// ❌ Грешка 1: Използване на capacity вместо size
for (int i = 0; i < arr.getCapacity(); i++)  // ГРЕШНО!
    cout << arr[i];  // Може да печата "боклук"

// ✅ ПРАВИЛНО
for (int i = 0; i < arr.getSize(); i++)
    cout << arr[i];

// ❌ Грешка 2: Лоша стратегия за растеж
new_capacity = capacity + 1;  // O(n) амортизирано - ЛОШО!

// ✅ ПРАВИЛНО
new_capacity = capacity * 2;  // O(1) амортизирано - ДОБРО!

// ❌ Грешка 3: Shallow copy
data = other.data;  // И двата сочат към едно място!
```

Note:
Разбирането на size vs capacity е критично. Лошата стратегия за растеж руши производителността.

---

<!-- .slide: data-background="#e8eaf6" -->

# Обобщение

---

## Ключови Изводи

<div class="columns">
<div class="column left">

**Концепции:**
- Size vs Capacity
- Реалокация при нужда
- Амортизирана O(1) сложност
- RAII принцип

</div>
<div class="column right">

**Практика:**
- Използвайте std::vector
- Разбирайте вътрешната структура
- Избягвайте memory leaks
- Правилно delete[]

</div>
</div>

**За изпита:**
- **Size vs Capacity** - задължително!
- **Амортизирана сложност** - удвояване vs константа
- **Memory management** - delete[] за масиви
- **Rule of Three** - деструктор, copy constructor, assignment

Note:
Тези концепции са фундаментални за всички структури от данни.

---

## Най-добри Практики

✅ **Винаги използвайте std::vector** в production код

✅ **Удвояване на капацитета** за оптимална амортизирана сложност

✅ **delete[] за масиви**, delete за единични обекти

✅ **Деструктор е задължителен** за предотвратяване на leaks

✅ **reserve()** когато знаете приблизителния размер

✅ **Разбирайте разликата** между size и capacity

Note:
Следването на тези практики ще ви спести много главоболие.

---

## Допълнителни Ресурси

**Dynamic Arrays:**
- [How do Dynamic Arrays Work? - GeeksforGeeks](https://www.geeksforgeeks.org/dsa/how-do-dynamic-arrays-work/)
- [Vectors in C++: Smart Dynamic Array](https://dev.to/saifulire/vectors-in-c-the-smart-dynamic-array-5ahe)

**STL:**
- [std::vector - cppreference](https://en.cppreference.com/w/cpp/container/vector)
- "Effective STL" by Scott Meyers

**Практика:**
- Имплементирайте собствен DynamicArray
- Добавете insert, erase, reserve
- Направете го generic с templates

Note:
Практиката е ключова за овладяване на материала.

---

<!-- .slide: data-background="#4caf50" -->

# Благодаря за Вниманието!

## Въпроси? 🎓

**Следваща лекция:** Списъци и Итератори

Note:
Време за въпроси! Не се притеснявайте да питате каквото и да е свързано с лекцията.
