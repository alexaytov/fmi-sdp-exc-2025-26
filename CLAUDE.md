# Claude Code Instructions - FMI SDP Course

Този файл съдържа инструкции за работа с курса по Структури от Данни и Програмиране чрез Claude Code.

## Структура на Проекта

```
fmi-sdp-exc-2025-26/
├── docusaurus-site/              # Активен Docusaurus сайт
│   ├── docs/                     # Лекционни материали
│   │   ├── 01-complexity-big-o/
│   │   │   ├── _category_.json
│   │   │   ├── lecture.md
│   │   │   └── exercises.md
│   │   ├── 02-compiler-optimizations/
│   │   └── ...
│   ├── src/                      # React компоненти
│   │   └── components/
│   └── docusaurus.config.ts     # Конфигурация
├── archived-original-lectures/   # Архив на оригинални файлове
└── .github/workflows/           # GitHub Actions за deployment

```

## Създаване на Нови Лекции от Текст

### Стъпка 1: Подготовка на материала

Когато получите текст за нова лекция (lecture.md или exc.md):

1. **Анализирайте структурата** на съществуващите лекции
2. **Идентифицирайте номера** на лекцията (например 11, 12, и т.н.)
3. **Определете темата** на лекцията

### Стъпка 2: Създаване на директория

```bash
# Формат на директорията: XX-topic-name
cd docusaurus-site/docs
mkdir XX-topic-name
```

Примери на naming convention:
- `11-hash-tables`
- `12-graphs-introduction`
- `13-graph-algorithms`

### Стъпка 3: Създаване на _category_.json

```json
{
  "label": "XX. Заглавие на Лекцията",
  "position": XX,
  "collapsed": false,
  "link": {
    "type": "generated-index",
    "description": "Кратко описание на темата"
  }
}
```

### Стъпка 4: Конвертиране на lecture.md

Създайте `lecture.md` със следната структура:

```markdown
---
title: "Лекция"
sidebar_position: 1
slug: lecture
tags: [tag1, tag2, tag3, data-structures, cpp]
---

import InfoBox from '@site/src/components/InfoBoxes/InfoBox';
import WarningBox from '@site/src/components/InfoBoxes/WarningBox';
import SuccessBox from '@site/src/components/InfoBoxes/SuccessBox';
import WhyBox from '@site/src/components/InfoBoxes/WhyBox';
import LearningObjectives from '@site/src/components/LearningObjectives';
import QuickSummary from '@site/src/components/QuickSummary';
import CollapsibleSection from '@site/src/components/CollapsibleSection';
import ComparisonBox from '@site/src/components/Comparison/ComparisonBox';
import Grid from '@site/src/components/Grid/Grid';
import Card from '@site/src/components/Grid/Card';

# Заглавие на Лекцията

<QuickSummary>

**Ключови познания от тази лекция:**

- **Точка 1**: Кратко резюме на основна концепция
- **Точка 2**: Важен insight или формула
- **Точка 3**: Практическо приложение
- **Точка 4**: Какво студентът ще може да прави след лекцията

</QuickSummary>

**Важно:** QuickSummary компонентът е **сгъваем** (collapsible). Студентите могат да го разгънат когато имат нужда от бърз преглед за изпита. По подразбиране е затворен, но може да се отвори с `defaultOpen={true}`.

<LearningObjectives
  objectives={[
    "Цел 1",
    "Цел 2",
    "Цел 3",
    "Цел 4"
  ]}
/>

---

[Съдържание на лекцията тук]

---

## Допълнителни Ресурси

### Онлайн Туториали

- [Линк 1](url) - Описание
- [Линк 2](url) - Описание

### Видео Лекции

- [Линк 1](url) - Описание

### Книги и Статии

- Книга/статия 1 - Описание

### Практически Задачи

- [Platform](url) - Описание
```

### Стъпка 5: Конвертиране на exercises.md

Създайте `exercises.md` със следната структура:

```markdown
---
sidebar_position: 2
title: "Упражнения"
tags: [exercises, practice, tag1, tag2]
---

import InfoBox from '@site/src/components/InfoBoxes/InfoBox';
import WarningBox from '@site/src/components/InfoBoxes/WarningBox';
import SuccessBox from '@site/src/components/InfoBoxes/SuccessBox';
import CollapsibleSection from '@site/src/components/CollapsibleSection';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import ExerciseCard from '@site/src/components/Exercise/ExerciseCard';
import ProgressTracker from '@site/src/components/Exercise/ProgressTracker';

# Упражнения: Заглавие на Темата

<ProgressTracker />

---

<InfoBox title="Информация за упражненията">

Този набор от упражнения покрива:
- Тема 1
- Тема 2
- Тема 3
- Тема 4

**Общо упражнения:** XX задачи в Y нива на сложност

</InfoBox>

---

## Лесни упражнения (EASY)

Фундаментални концепции и базови разбирания

<ExerciseCard difficulty="easy">

### Заглавие на задачата

Описание на задачата...

<CollapsibleSection title="Подсказка" icon="💡">

Подсказка за решение...

</CollapsibleSection>

<CollapsibleSection title="Решение" icon="✅">

```cpp
// Код на решението
```

**Обяснение:**
- Точка 1
- Точка 2

</CollapsibleSection>

</ExerciseCard>

<ExerciseCard difficulty="easy">

### Тест с многовариантни отговори

**Въпрос:** Какво е...?

- A) Отговор 1
- B) Отговор 2
- C) Отговор 3
- D) Отговор 4

<CollapsibleSection title="Отговор" icon="✅">

**Отговор: B) Отговор 2**

Обяснение защо...

</CollapsibleSection>

</ExerciseCard>

---

## Лесни-Средни упражнения (EASY-MEDIUM)

Изграждане на разбиране и приложение на концепциите

<ExerciseCard difficulty="easy-medium">

### Заглавие на задачата

Описание...

<CollapsibleSection title="Решение" icon="✅">

**Обяснение:**

<Tabs>
<TabItem value="option1" label="Вариант 1" default>

Описание на първия вариант...

```cpp
// Код за вариант 1
```

**Предимства:**
- ✅ Предимство 1
- ✅ Предимство 2

**Недостатъци:**
- ❌ Недостатък 1

</TabItem>
<TabItem value="option2" label="Вариант 2">

Описание на втория вариант...

```cpp
// Код за вариант 2
```

</TabItem>
</Tabs>

</CollapsibleSection>

</ExerciseCard>

---

## Средни упражнения (MEDIUM)

Приложение и анализ на концепциите

<ExerciseCard difficulty="medium">

### Заглавие на задачата

Описание на по-сложна задача...

```cpp
// Код скелет ако е нужен
void function_name() {
    // Вашият код тук
}
```

<CollapsibleSection title="Подсказка" icon="💡">

Помислете за...

</CollapsibleSection>

<CollapsibleSection title="Решение" icon="✅">

```cpp
// Пълно решение
```

**Анализ на решението:**

| Критерий | Стойност |
|----------|----------|
| Времева сложност | O(n) |
| Пространствена сложност | O(1) |

</CollapsibleSection>

</ExerciseCard>

---

## Средни-Трудни упражнения (MEDIUM-HARD)

Напреднало приложение и експериментиране

<ExerciseCard difficulty="medium-hard">

### Заглавие на задачата

Описание на напреднала задача...

<CollapsibleSection title="Подсказка" icon="💡">

Подсказка...

</CollapsibleSection>

<CollapsibleSection title="Решение" icon="✅">

**Решение:**

```cpp
// Код на решението
```

**Performance анализ:**

- **Оптимизация 1**: Обяснение
- **Оптимизация 2**: Обяснение

</CollapsibleSection>

</ExerciseCard>

---

## Трудни упражнения (HARD)

Комплексен анализ и решаване на проблеми

<ExerciseCard difficulty="hard">

### Заглавие на задачата

Описание на трудна задача, която изисква дълбоко разбиране...

<CollapsibleSection title="Подсказка" icon="💡">

Разгледайте...

</CollapsibleSection>

<CollapsibleSection title="Решение" icon="✅">

**Подробно решение:**

<Tabs>
<TabItem value="approach1" label="Подход 1" default>

Описание на първия подход...

```cpp
// Имплементация
```

**Предимства:**
- ✅ Предимство 1

**Недостатъци:**
- ❌ Недостатък 1

</TabItem>
<TabItem value="approach2" label="Подход 2">

Описание на втория подход...

```cpp
// Имплементация
```

</TabItem>
</Tabs>

**Сравнителен анализ:**

| Критерий | Подход 1 | Подход 2 |
|----------|----------|----------|
| Време | O(n log n) | O(n²) |
| Памет | O(n) | O(1) |

**Практически съвети:**
1. Съвет 1
2. Съвет 2

</CollapsibleSection>

</ExerciseCard>

---

## Допълнителни ресурси

<InfoBox title="Полезни ресурси за практика">

**Инструменти:**
- [Tool 1](url) - Описание
- [Tool 2](url) - Описание

**Практически съвети:**
- Съвет 1
- Съвет 2

</InfoBox>

<SuccessBox title="Ключови точки за запомняне">

- **Точка 1**: Обяснение
- **Точка 2**: Обяснение
- **Точка 3**: Обяснение

</SuccessBox>
```

### Формат на упражненията - Best Practices

#### Структура по нива на сложност

Организирайте упражненията в **5 нива на сложност**:

1. **EASY** - Фундаментални концепции
   - Прости дефиници
   - Базов код
   - Тестове с многовариантни отговори
   - 5-15 минути/задача

2. **EASY-MEDIUM** - Изграждане на разбиране
   - Сравнения между подходи
   - Обяснения на концепции
   - Прости имплементации
   - 15-25 минути/задача

3. **MEDIUM** - Приложение
   - Имплементация на структури/алгоритми
   - Анализ на производителност
   - Сравнения на контейнери
   - 25-40 минути/задача

4. **MEDIUM-HARD** - Напреднали концепции
   - Оптимизации
   - Експериментиране
   - Compiler Explorer анализ
   - 40-60 минути/задача

5. **HARD** - Комплексни проблеми
   - Проектиране на системи
   - Performance анализ
   - Benchmarking
   - 60+ минути/задача

#### Компоненти за използване

1. **`<ExerciseCard difficulty="...">`**
   - Винаги задавай `difficulty` prop
   - Може да добавиш `timeEstimate` и `tags`
   - **НЕ** задавай `id` (автоматично генериране)

2. **`<CollapsibleSection>`**
   - Използвай за подсказки: `title="Подсказка" icon="💡"`
   - Използвай за решения: `title="Решение" icon="✅"`
   - Използвай за отговори: `title="Отговор" icon="✅"`

3. **`<Tabs>` и `<TabItem>`**
   - Отлични за сравняване на подходи
   - Използвай за различни имплементации
   - Добави `default` на първия tab

4. **`<InfoBox>`, `<SuccessBox>`, `<WarningBox>`**
   - За важни забележки
   - За обобщения
   - За предупреждения

#### Формат на решенията

Всяко решение трябва да съдържа:

1. **Код** - Пълна работеща имплементация
2. **Обяснение** - Защо работи, как работи
3. **Анализ** - Времева/пространствена сложност
4. **Предимства/Недостатъци** - С емодзи (✅/❌)
5. **Практически съвети** (за трудни задачи)

#### Типове задачи

1. **Теория** - Дефиниции, обяснения
2. **Multiple Choice** - Тестове с отговори
3. **Имплементация** - Напиши функция/клас
4. **Анализ** - Анализирай даден код
5. **Оптимизация** - Подобри производителността
6. **Benchmark** - Измери и сравни
7. **Debugging** - Намери и оправи грешки
8. **Design** - Проектирай система/структура

#### Примери за заглавия

- **EASY**: "Какво е показалец в C++?"
- **EASY**: "Дефиниция на пространствена локалност"
- **EASY-MEDIUM**: "Временна vs Пространствена локалност"
- **MEDIUM**: "Сравнение на контейнери"
- **MEDIUM**: "Loop Unrolling"
- **MEDIUM-HARD**: "Compiler Explorer анализ"
- **HARD**: "Performance comparison: Arrays vs Containers"
- **HARD**: "Cache-oblivious matrix transpose"

#### Обяснения и коментари

- Използвай **български език** за обяснения
- Използвай **емодзи** стратегически (✅ ❌ ⭐ ⚠️ 💡)
- Форматирай таблици за сравнения
- Добавяй **"Защо работи"** секции
- Включвай **практически примери**
- Посочвай **често срещани грешки**

#### Код примери

```cpp
// ✅ Добър пример - с коментари
void good_function() {
    int sum = 0;  // Инициализация
    for (int i = 0; i < n; i++) {
        sum += arr[i];  // Натрупване
    }
    return sum;
}

// ❌ Лош пример - обяснено защо
void bad_function() {
    for (int i = 0; i < n; i += 100) {
        // Лоша пространствена локалност - скача през паметта!
    }
}
```

### Стъпка 6: Важни Компоненти

#### Налични React Компоненти:

1. **InfoBox** - Информационни кутии
   ```jsx
   <InfoBox title="Заглавие">
   Съдържание
   </InfoBox>
   ```

2. **SuccessBox** - Успешни съобщения
   ```jsx
   <SuccessBox title="Заглавие">
   Съдържание
   </SuccessBox>
   ```

3. **WarningBox** - Предупреждения
   ```jsx
   <WarningBox title="Заглавие">
   Съдържание
   </WarningBox>
   ```

4. **WhyBox** - "Защо?" обяснения
   ```jsx
   <WhyBox title="Защо е важно?">
   Обяснение
   </WhyBox>
   ```

5. **CollapsibleSection** - Сгъваеми секции
   ```jsx
   <CollapsibleSection title="Заглавие" icon="🎯">
   Съдържание
   </CollapsibleSection>
   ```

6. **Grid и Card** - Grid layout
   ```jsx
   <Grid columns={2}>
     <Card title="Заглавие 1">
     Съдържание 1
     </Card>
     <Card title="Заглавие 2">
     Съдържание 2
     </Card>
   </Grid>
   ```

7. **ComparisonBox** - Сравнения
   ```jsx
   <ComparisonBox
     left={{ title: "Подход 1", content: "..." }}
     right={{ title: "Подход 2", content: "..." }}
   />
   ```

8. **LearningObjectives** - Цели на обучението
   ```jsx
   <LearningObjectives
     objectives={["Цел 1", "Цел 2"]}
   />
   ```

9. **ExerciseCard и ProgressTracker** - Автоматична система за проследяване на прогреса

   **✨ ZERO-CONFIG РЕШЕНИЕ С REACT CONTEXT (Декември 2025)**

   Системата за упражнения е напълно автоматична и използва **React Context API** за централизирано управление на състоянието. **НЕ е нужна ръчна конфигурация!**

   **🎯 Препоръчан Workflow (Zero Config):**

   ```jsx
   # Упражнения - Графи

   <ProgressTracker />

   ---

   ### Задача 1: Терминология на Графите

   <ExerciseCard
     difficulty="easy"
     timeEstimate="15 min"
     tags={["theory", "terminology"]}
   >

   Дефинирайте следните термини...

   </ExerciseCard>
   ```

   **Как работи автоматичната система:**

   1. **ExerciseProvider Context**: Всички MDX страници се обвиват автоматично с `<ExerciseProvider>` чрез Docusaurus theme swizzling (`src/theme/MDXContent/index.tsx`)

   2. **Автоматична регистрация**: `ExerciseCard` автоматично се регистрира в Context при mount:
      - Търси най-близкото предходно H2/H3 заглавие
      - Извлича заглавието: "Задача 1: Терминология на Графите"
      - Премахва префикса → "Терминология на Графите"
      - Транслитерира на английски → "Terminologiya na Grafite"
      - Конвертира в kebab-case → `"terminologiya-na-grafite"`
      - Генерира storage ключ: `${pathname}_exercise_terminologiya-na-grafite`

   3. **ProgressTracker**: Автоматично чете от Context и показва прогреса - **НЕ е нужен `exercises` prop!**

   4. **LocalStorage**: Прогресът се съхранява автоматично в browser LocalStorage

   **⚠️ КРИТИЧНО: Избягване на Infinite Loops**

   При работа с Context и useEffect hooks, **трябва да внимаваш за infinite loops!**

   **❌ ГРЕШНО (Infinite Loop):**
   ```jsx
   const context = useExerciseContext();

   useEffect(() => {
     context.registerExercise(difficulty, id);
   }, [difficulty, context]); // ❌ context обект като dependency!
   ```

   **Проблем:** Context обектът се променя при всяка промяна на `exercises` array-а → useEffect се изпълнява → registerExercise → exercises се променят → loop!

   **✅ ПРАВИЛНО (Destructure Functions):**
   ```jsx
   const context = useExerciseContext();
   const { registerExercise, unregisterExercise, getStorageKey } = context;

   useEffect(() => {
     registerExercise(difficulty, id);
     return () => unregisterExercise(id);
   }, [difficulty, registerExercise, unregisterExercise]); // ✅ Стабилни функции!
   ```

   **Защо работи:** Функциите са мemoized с `useCallback` със стабилни dependencies, така че те НЕ се променят при всяко re-render.

   **⚠️ ВАЖНИ ПРАВИЛА:**

   1. **НЕ използвай context обект като dependency** - винаги destructure функциите!
   2. **НЕ съхранявай pathname в state** - използвай `getPathname()` callback вместо това
   3. **Memoize context value** с `useMemo` за да намалиш ненужните re-renders
   4. **Използвай useCallback** за функции в Context с празен/стабилен dependency array

   **🏗️ Архитектура:**

   ```
   MDXContent (wrapper)
   └── ExerciseProvider (Context)
       ├── exercises: ExerciseInfo[]
       ├── registerExercise(difficulty, id)
       ├── unregisterExercise(id)
       └── getStorageKey(id)
           │
           ├── ProgressTracker (чете от Context)
           │   └── Показва: X/Y completed, percentage
           │
           └── ExerciseCard (регистрира се в Context)
               ├── Auto-extracts heading
               ├── Auto-generates semantic ID
               ├── Registers with Context
               └── Saves completion to LocalStorage
   ```

   **💡 Best Practices:**

   - **НЕ задавай `exercises` prop на ProgressTracker** - автоматично!
   - **НЕ задавай `id` prop на ExerciseCard** - автоматично!
   - **Пиши стабилни заглавия** - променянето на заглавие променя ID
   - **Използвай описателни заглавия на български**
   - **Destructure функции от Context** - избягвай infinite loops!

   **⚠️ Backward Compatibility:**
   - Може да задаваш `id` prop ръчно (за миграция на стари exercises)
   - Ако `id` е зададен, той ще се използва вместо auto-generated

   **🔧 Troubleshooting:**

   - **"useExerciseContext must be used within ExerciseProvider"**: Изчисти `.docusaurus` cache
   - **"Maximum update depth exceeded"**: Проверете дали използвате context обект като dependency вместо destructured функции
   - **Прогресът не се актуализира**: Проверете дали `window.dispatchEvent(new Event('exerciseProgressChanged'))` се извиква след промяна

### Стъпка 7: Добавяне на Ресурси

**ВАЖНО**: Към края на всяка лекция добавете секция "Допълнителни Ресурси" с:

- **Онлайн Туториали**: GeeksforGeeks, TutorialsPoint, W3Schools
- **Видео Лекции**: YouTube, академични курсове
- **Визуализации**: Интерактивни инструменти
- **Практически Задачи**: LeetCode, HackerRank, CodeForces
- **Книги**: Препоръки за четене
- **Документация**: C++ reference, STL docs

Използвайте следния шаблон:

```markdown
## Допълнителни Ресурси

### Онлайн Туториали

- [Ресурс 1](url) - Кратко описание
- [Ресурс 2](url) - Кратко описание

### Визуализация и Примери

- [Tool 1](url) - Описание

### Практически Задачи

- [Platform - Topic](url) - Описание

### Книги

- "Книга 1" - Автор, глави/секции
```

### Стъпка 8: Code Snippets

Използвайте markdown code blocks с език:

```cpp
#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
```

### Стъпка 9: Математически Формули

За математически формули използвайте LaTeX синтаксис:

**⚠️ ВАЖНО: MDX v3 LaTeX Escaping Правило**

В MDX v3 (което Docusaurus използва), **ВСИЧКИ къдрави скоби `{` и `}` в LaTeX формули ТРЯБВА да се escape-ват** с backslash (`\{` и `\}`), защото иначе MDX ги интерпретира като JSX expressions!

**✅ ПРАВИЛНО:**
```markdown
Времева сложност: $O(n \log n)$

$$
T(n) = \begin\{cases\}
  O(1) & \text\{ако \} n = 1 \\
  2T(n/2) + O(n) & \text\{иначе\}
\end\{cases\}
$$

$$
\text\{parent\}(i) = \left\lfloor \frac\{i-1\}\{4\} \right\rfloor
$$

$$
\sum_\{i=0\}^\{h\} \frac\{n\}\{2^\{i+1\}\} \times (h - i)
$$

### Graph Density
- **Dense:** Many edges (close to maximum possible \(\frac\{n(n-1)\}\{2\}\)).
```

**❌ ГРЕШНО** (ще предизвика React 
JSX грешка):
```markdown

### Graph Density
- **Dense:** Many edges (close to maximum possible \(\frac{n(n-1)}{2}\)).

$$
T(n) = \begin{cases}    <!-- { без \ -->
  O(1) & \text{ако } n = 1 \\
  2T(n/2) + O(n) & \text{иначе}
\end{cases}
$$
```

**Често срещани грешки:**
- `\frac{n}{2}` ❌ → `\frac\{n\}\{2\}` ✅
- `\text{parent}` ❌ → `\text\{parent\}` ✅
- `\sum_{i=0}^{h}` ❌ → `\sum_\{i=0\}^\{h\}` ✅
- `2^{h+1}` ❌ → `2^\{h+1\}` ✅

Код в cpp блок не трябва да се escape-ва:

ТОВА Е ВЯРНО:
```cpp
int binarySearch(const vector<int>& arr, int target) {
    int low = 0;
    int high = arr.size() - 1;

    while (low <= high) {
        int mid = low + (high - low) / 2; // Избягва overflow!

        if (arr[mid] == target) return mid;
        else if (arr[mid] < target) low = mid + 1;
        else high = mid - 1;
    }
    return -1; // Не е намерен
}
```

**Типична грешка в browser console:**
```
Error: Objects are not valid as a React child (found: [object Window])
```

Това означава, че има неescaped `{` или `}` в LaTeX формула!

**⚠️ ВАЖНО: MDX Escaping за `<<` и `>>` Символи**

В MDX, символите `<<` и `>>` се интерпретират като JSX тагове и **ТРЯБВА да се escape-ват** извън code blocks!

**✅ ПРАВИЛНО:**
```markdown
- Разредени графи (E &lt;&lt; V²)
- Битово преместване надясно (x &gt;&gt; 2)
```

**❌ ГРЕШНО:**
```markdown
- Разредени графи (E << V²)  <!-- Ще предизвика MDX грешка! -->
```

**Важно:** В markdown code blocks (обградени с \`\`\`) не е нужно escape-ване:
```markdown
\`\`\`cpp
cout << "Hello" << endl;  // Това е ОК в code block
x = x >> 2;                // Това също е ОК
\`\`\`
```

**Типична грешка:**
```
Error: Unexpected character `<` (U+003C) before name, expected a character
that can start a name, such as a letter, `$`, or `_`
```

Това означава, че има неescaped `<<` или `>>` извън code block!

### Стъпка 10: Тестване локално

```bash
cd docusaurus-site
npm start
```

Отворете: http://localhost:3000

### Стъпка 11: Commit и Push

```bash
git add docusaurus-site/docs/XX-topic-name/
git commit -m "Add lecture XX: Topic Name

- Add lecture.md with comprehensive content
- Add exercises.md with practice problems
- Include educational resources
- Add interactive components

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
git push origin main
```

## Създаване на Reveal.js Презентации

### Какво е Reveal.js?

Reveal.js е библиотека за създаване на HTML презентации. Курсът използва автоматична система за генериране на презентации от markdown файлове.

### Структура на презентацията

Всяка лекция може да има допълнителен `slides.md` файл:

```
docs/
└── XX-topic-name/
    ├── _category_.json
    ├── lecture.md
    ├── exercises.md
    └── slides.md          # Презентация (опционална)
```

### Базова структура на slides.md

```markdown
---
title: Заглавие на Презентацията
theme: white
highlightTheme: github
transition: slide
controls: true
progress: true
slideNumber: true
---

# Заглавие на Презентацията

Подзаглавие или описание

---

## Слайд 2

Съдържание на втория слайд

---

## Слайд 3

Още съдържание
```

### Разделители на слайдове

- **`---`** (три тирета) - Създава хоризонтален слайд (напред/назад със стрелки ← →)
- **`--`** (две тирета) - Създава вертикален под-слайд (нагоре/надолу със стрелки ↑ ↓)

**Пример:**

```markdown
---

## Основна тема

Това е основен слайд

--

### Подтема 1

Вертикален под-слайд (↓)

--

### Подтема 2

Още един вертикален под-слайд (↓)

---

## Следваща основна тема

Нов хоризонтален слайд (→)
```

### Speaker Notes

Добавете бележки за лектора с `Note:`:

```markdown
---

## Слайд със бележки

Видимо съдържание за студентите

Note:
Тези бележки се виждат само в speaker view (натиснете 'S').
Можете да пишете какво да кажете на този слайд.
```

**Как да видите speaker notes:**
- Натиснете клавиш **S** в презентацията
- Отваря се отделен прозорец със:
  - Текущ слайд
  - Следващ слайд
  - Speaker notes
  - Таймер

### Code Blocks

Код се оцветява автоматично с highlight.js:

```markdown
---

## Пример с Код

\`\`\`cpp
int binarySearch(int arr[], int n, int target) {
    int low = 0, high = n - 1;
    while (low <= high) {
        int mid = low + (high - low) / 2;
        if (arr[mid] == target) return mid;
        if (arr[mid] < target) low = mid + 1;
        else high = mid - 1;
    }
    return -1;
}
\`\`\`

**Сложност:** O(log n)
```

### Изображения

Добавяйте изображения с обикновен markdown синтаксис:

```markdown
---

## Слайд с Изображение

![Описание](https://example.com/image.png)

- Изображенията се центрират автоматично
- Максимален размер: 70% width, 400px height
```

### Двуколонен Layout

Използвайте HTML `<div>` тагове за двуколонна структура (текст отляво, изображение отдясно):

```markdown
---

## Двуколонен Слайд

<div class="columns">
<div class="column left">

### Текст отляво

- Точка 1
- Точка 2
- Точка 3

\`\`\`cpp
// Код тук
int x = 42;
\`\`\`

</div>
<div class="column right">

![Визуализация](https://example.com/diagram.png)

</div>
</div>
```

**CSS класове:**
- `.columns` - Контейнер за двуколонен layout (flexbox)
- `.column` - Една колона (flex: 1)
- `.column.left` - Лява колона с текст (text-align: left)
- `.column.right` - Дясна колона с изображение (text-align: center)

**Важно:** Оставяйте празни редове преди и след markdown съдържание в `<div>` таговете!

### Фонове и атрибути на слайдове

Можете да добавите специални атрибути към слайдове:

```markdown
---

<!-- .slide: data-background="#4d7e65" -->

## Слайд с Цветен Фон

Зелен фон

---

<!-- .slide: data-background="https://example.com/bg.jpg" -->

## Слайд с Фоново Изображение

---

<!-- .slide: data-transition="zoom" -->

## Специална Анимация

Zoom transition само за този слайд
```

### Таблици

```markdown
---

## Таблица

| Колона 1 | Колона 2 | Колона 3 |
|----------|----------|----------|
| Ред 1    | Данни    | Още      |
| Ред 2    | Данни    | Още      |
```

### Списъци

```markdown
---

## Списъци

**Неномериран:**
- Точка 1
- Точка 2
  - Подточка
  - Подточка

**Номериран:**
1. Първо
2. Второ
3. Трето
```

### Формули (LaTeX)

**⚠️ ВАЖНО:** В MDX трябва да escape-вате къдрави скоби в LaTeX:

```markdown
---

## Математически Формули

Inline: $O(n \log n)$

Block:
$$
T(n) = \begin\{cases\}
  O(1) & \text\{ако \} n = 1 \\
  2T(n/2) + O(n) & \text\{иначе\}
\end\{cases\}
$$
```

**Често срещани грешки:**
- `\frac{n}{2}` ❌ → `\frac\{n\}\{2\}` ✅
- `\sum_{i=0}^{n}` ❌ → `\sum_\{i=0\}^\{n\}` ✅

### Rebuild на Презентации

След промени в `slides.md` файлове, rebuild-нете презентациите:

```bash
cd docusaurus-site
npm run build:slides
```

**Какво прави тази команда:**
1. Сканира всички `slides.md` файлове в `docs/`
2. Конвертира markdown към HTML
3. Генерира standalone reveal.js HTML файлове
4. Записва ги в `static/slides/[topic-name]/index.html`

**Важно:** `npm start` НЕ хваща промени в slides автоматично! Винаги използвайте `npm run build:slides`.

### Keyboard Shortcuts

Когато гледате презентацията:

- **Стрелки (← → ↑ ↓)** - Навигация между слайдове
- **Space** - Следващ слайд
- **S** - Speaker notes view (отделен прозорец)
- **F** - Fullscreen mode
- **ESC** - Overview mode (показва всички слайдове)
- **?** - Показва всички keyboard shortcuts
- **B** / **.** - Blackout (изключва екрана)

### PDF Export

За да експортирате презентацията като PDF:

1. Добавете `?print-pdf` към URL-а
   - Пример: `http://localhost:3000/slides/complexity-big-o/?print-pdf`
2. Използвайте Chrome браузър
3. File → Print → Save as PDF
4. Настройки:
   - Layout: Landscape
   - Margins: None
   - Background graphics: Checked

### Frontmatter Опции

```markdown
---
title: "Заглавие"              # Показва се в <title> tag
theme: white                   # white, black, league, beige, sky, night, serif, simple, solarized
highlightTheme: github         # github, monokai, vs, atom-one-dark
transition: slide              # slide, fade, convex, concave, zoom
controls: true                 # Показва контролите долу-дясно
progress: true                 # Показва progress bar долу
slideNumber: true              # Показва номер на слайда
---
```

### Best Practices

1. **Кратки слайдове** - Не препълвайте слайдовете с текст
2. **Визуални елементи** - Използвайте изображения, диаграми, gif-ове
3. **Code highlighting** - Винаги указвайте езика в code blocks (\`\`\`cpp)
4. **Speaker notes** - Добавяйте бележки за важни обяснения
5. **Двуколонен layout** - Използвайте за код + визуализация
6. **Консистентност** - Следвайте стила на съществуващите презентации
7. **Тестване** - Винаги прегледайте презентацията преди commit

### Примерен Workflow

```bash
# 1. Редактирай slides.md
code docs/01-complexity-big-o/slides.md

# 2. Rebuild презентациите
npm run build:slides

# 3. Refresh browser
# Отвори http://localhost:3000/slides/complexity-big-o/

# 4. Провери резултата и повтори 1-3 при нужда

# 5. Commit промените
git add docs/01-complexity-big-o/slides.md
git commit -m "Update slides for complexity-big-o

- Added new diagrams
- Improved code examples
- Added speaker notes

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

### Troubleshooting

**Проблем:** Промените не се виждат след refresh
**Решение:** Изпълни `npm run build:slides` отново - `npm start` не хваща slides промени

**Проблем:** Кодът не е оцветен
**Решение:** Провери че си указал езика след \`\`\` (напр. \`\`\`cpp)

**Проблем:** Изображението излиза извън слайда
**Решение:** Използвай двуколонен layout или намали размера на изображението

**Проблем:** Таблицата не се показва правилно
**Решение:** Провери че има празни редове преди и след таблицата

**Проблем:** LaTeX формулите не се показват
**Решение:** Escape-ни всички `{` и `}` с backslash: `\{` и `\}`

## Naming Conventions

### Директории
- Формат: `XX-topic-name-in-lowercase`
- Примери: `11-hash-tables`, `12-graph-algorithms`

### Файлове
- `_category_.json` - Метаданни за категорията
- `lecture.md` - Лекционен материал
- `exercises.md` - Практически задачи

### Tags
- Общи: `data-structures`, `algorithms`, `cpp`, `practice`, `exercises`
- Специфични: `hash-tables`, `graphs`, `trees`, `sorting`, и т.н.

## React Context Architecture - Exercise Tracking System

### Автоматично Генериране и Управление на Упражнения

От **декември 2025**, цялата система за упражнения е **напълно автоматична** и базирана на **React Context API**.

**🎯 Zero Config Approach:**
- **НЕ** е нужно да задаваш `id` prop на `<ExerciseCard>`
- **НЕ** е нужно да задаваш `exercises` prop на `<ProgressTracker>`
- Всичко работи автоматично чрез Context!

### Техническа Имплементация

**1. ExerciseContext.tsx** - Централизирано управление на състоянието

```typescript
interface ExerciseInfo {
  id: string;           // Semantic ID
  difficulty: string;   // easy, medium, hard
  pathname: string;     // URL path за namespace
}

interface ExerciseContextType {
  exercises: ExerciseInfo[];
  registerExercise: (difficulty: string, semanticId: string) => void;
  unregisterExercise: (id: string) => void;
  getStorageKey: (id: string) => string;
}
```

**Критични имплементационни детайли:**
- `getPathname()` е `useCallback` функция, **НЕ** state (избягва re-renders)
- `registerExercise`, `unregisterExercise`, `getStorageKey` са memoized с `useCallback`
- Context value е memoized с `useMemo`
- `transliterateBulgarian()` и `slugify()` за генериране на IDs

**2. ExerciseCard.tsx** - Автоматична регистрация

**Auto-ID Generation Flow:**
1. `useEffect` при mount търси предходно H2/H3 заглавие с DOM traversal
2. Извлича текст: "Задача 1: Терминология на Графите"
3. Премахва префикс: "Терминология на Графите"
4. `slugify()` → `"terminologiya-na-grafite"`
5. `registerExercise(difficulty, id)` - регистрация в Context
6. Cleanup: `unregisterExercise(id)` при unmount

**⚠️ КРИТИЧНО - Правилни Dependencies:**
```typescript
// ✅ ПРАВИЛНО
const { registerExercise, unregisterExercise, getStorageKey } = context;

useEffect(() => {
  registerExercise(difficulty, id);
  return () => unregisterExercise(id);
}, [difficulty, registerExercise, unregisterExercise]); // Стабилни функции
```

**3. ProgressTracker.tsx** - Автоматично показване на прогреса

```typescript
const context = useExerciseContext();

const calculateProgress = () => {
  context.exercises.forEach(exercise => {
    const key = context.getStorageKey(exercise.id);
    if (localStorage.getItem(key) === 'true') count++;
  });
};
```

- Слуша за `exerciseProgressChanged` events
- Re-calculates при промяна на `context.exercises`
- Показва процент и брой завършени упражнения

**4. MDXContent Wrapper** - Автоматично обвиване

Docusaurus theme swizzle осигурява че всички MDX страници са обвити с Context:

```typescript
// src/theme/MDXContent/index.tsx
export default function MDXContentWrapper(props: Props): React.ReactElement {
  return (
    <ExerciseProvider>
      <MDXContent {...props} />
    </ExerciseProvider>
  );
}
```

### Миграция от Старо Решение

**Ако имаш стари exercises.md файлове с `exercises` prop:**

1. **Премахни `exercises` array от ProgressTracker:**
   ```markdown
   <!-- СТАРО -->
   <ProgressTracker exercises={[...]} />

   <!-- НOВО -->
   <ProgressTracker />
   ```

2. **Премахни `id` props от ExerciseCard-ове** (опционално):
   ```markdown
   <!-- СТАРО -->
   <ExerciseCard id="terminologiya-na-grafite" difficulty="easy">

   <!-- НОВО (автоматично генериране) -->
   <ExerciseCard difficulty="easy">
   ```

3. **Изчисти Docusaurus cache:**
   ```bash
   rm -rf docusaurus-site/.docusaurus
   npm start
   ```

### Troubleshooting Common Issues

**Issue 1: "useExerciseContext must be used within ExerciseProvider"**
- **Причина:** Docusaurus cache не е актуализиран
- **Решение:** `rm -rf .docusaurus && npm start`

**Issue 2: "Maximum update depth exceeded" (Infinite Loop)**
- **Причина:** Context обект като dependency в useEffect
- **Решение:** Destructure функциите от context
- **Пример:**
  ```typescript
  // ❌ ГРЕШНО
  }, [context]);

  // ✅ ПРАВИЛНО
  const { registerExercise } = context;
  }, [registerExercise]);
  ```

**Issue 3: Прогресът не се актуализира**
- **Причина:** `exerciseProgressChanged` event не се извиква
- **Решение:** След промяна в localStorage, извикай:
  ```typescript
  window.dispatchEvent(new Event('exerciseProgressChanged'));
  ```

## Best Practices

### Exercise System Best Practices

1. **Zero Config Approach** - НЕ задавай `id` на ExerciseCard и `exercises` на ProgressTracker
2. **Destructure Context Functions** - Винаги `const { registerExercise } = context;` за да избегнеш infinite loops
3. **Стабилни заглавия** - Променянето на заглавие променя ID и губи прогрес
4. **Описателни заглавия** - Използвай ясни български заглавия за автоматичното ID generation

### General Development Best Practices

5. **Използвай React компоненти** вместо обикновен markdown за по-добър UX
6. **Добавяй ресурси** към всяка лекция (tutorials, videos, practice problems)
7. **Включвай примери** с работещ C++ код
8. **Създавай визуализации** където е възможно (диаграми, схеми)
9. **Тествай локално** преди commit (`npm start`)
10. **Следвай naming conventions** за консистентност (XX-topic-name)
11. **Добавяй tags** за по-добра навигация и search
12. **Пиши на български** за съдържанието на лекциите
13. **Използвай английски** за код, променливи и технически термини
14. **Escape LaTeX брейсове** в MDX (`\{` и `\}`)
15. **Escape comparison operators** извън code blocks (`&lt;&lt;`, `&gt;&gt;`)
16. **Изчиствай cache** при странни грешки (`rm -rf .docusaurus`)

## Troubleshooting

### Build Failed
```bash
cd docusaurus-site
npm run build
# Провери грешките в output-а
```

### Broken Links
- Провери че всички линкове са правилни
- Използвай относителни пътища за вътрешни линкове
- Тествай линковете локално

### Компоненти не се зареждат
- Провери import statements
- Провери че paths са правилни: `@site/src/components/...`
- Рестартирай dev server

## Deployment

Deployment е автоматичен чрез GitHub Actions:
- Push към `main` branch → автоматичен build и deploy
- Провери статус: https://github.com/alexaytov/fmi-sdp-exc-2025-26/actions
- Live site: https://alexaytov.github.io/fmi-sdp-exc-2025-26/

## Полезни Команди

```bash
# Стартиране на dev server
cd docusaurus-site && npm start

# Build за production
cd docusaurus-site && npm run build

# Serve production build локално
cd docusaurus-site && npm run serve

# Проверка за broken links
cd docusaurus-site && npm run build

# Clear cache
cd docusaurus-site && npm run clear
```

---

## Създаване на Презентации с Reveal.js

### Overview

Курсът поддържа интерактивни презентации базирани на **reveal.js**. Всяка лекция може да има съпътстваща презентация със слайдове, която се генерира автоматично от markdown файл.

### Структура

```
docs/XX-topic-name/
├── _category_.json
├── lecture.md          # Лекционни материали
├── exercises.md        # Упражнения
└── slides.md           # Презентация (НОВО)
```

### Quick Start

1. **Създайте `slides.md`** в директорията на лекцията
2. **Добавете frontmatter и слайдове**
3. **Build & Deploy** - презентацията се генерира автоматично
4. **Navbar се актуализира автоматично** - новата презентация се появява в dropdown менюто

### Формат на slides.md

```markdown
---
title: Заглавие на Презентацията
theme: white
highlightTheme: github
transition: slide
---

# Първи Слайд

Съдържание на първия слайд

---

# Втори Слайд

Съдържание на втория слайд

Note:
Това е speaker note - вижда се само в speaker view (натисни 'S')

---

## Слайд с Код

\`\`\`cpp
int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
\`\`\`

---

<!-- .slide: data-background="#2c5282" -->

## Слайд с цветен фон

Можете да добавяте атрибути към слайдовете

--

### Вертикален подслайд

Използвайте `--` за вертикални подслайдове

---

## Фрагменти (incremental reveals)

<!-- .element: class="fragment" -->
Първо се появява това

<!-- .element: class="fragment fade-in" -->
После това с fade-in ефект

<!-- .element: class="fragment highlight-red" -->
И накрая това се оцветява в червено
```

### Frontmatter Options

| Опция | Стойност по подразбиране | Описание |
|-------|--------------------------|----------|
| `title` | (задължителна) | Заглавие на презентацията |
| `theme` | `white` | Reveal.js тема (white, black, league, etc.) |
| `highlightTheme` | `github` | Тема за код highlighting |
| `transition` | `slide` | Transition ефект (slide, fade, convex, etc.) |
| `controls` | `true` | Показва ли стрелките за навигация |
| `progress` | `true` | Показва ли прогрес бара |
| `slideNumber` | `true` | Показва ли номерата на слайдовете |

### Автоматична Navbar Интеграция

**ВАЖНО:** Navbar менюто "Презентации" се актуализира **автоматично** при добавяне на нови `slides.md` файлове!

**Как работи:**

1. Plugin-ът (`reveal-slides-plugin.js`) сканира `docs/` директорията при build
2. Намира всички `XX-topic-name/slides.md` файлове
3. Извлича `lectureNumber`, `lectureSlug` и `lectureTitle` от frontmatter
4. `docusaurus.config.ts` използва функцията `getSlidesForNavbar()` за генериране на navbar items
5. Резултат: Dropdown менюто се попълва автоматично без ръчна конфигурация!

**Пример:**

```typescript
// В docusaurus.config.ts (ВЕЧЕ КОНФИГУРИРАНО)
const { getSlidesForNavbar } = require('./plugins/reveal-slides-plugin.js');
const slides = getSlidesForNavbar(path.join(__dirname, 'docs'));

// Автоматично генерирани navbar items:
// 01. Концепции за Сложност, Big-O Нотация, Тестване и Double Precision в C++
// 03. Масиви и Двоично Търсене
// ---
// Всички презентации →
```

**НЕ е нужно** да редактираш ръчно navbar конфигурацията когато добавяш нова презентация!

### Slide Delimiters

- **`---`** - Нов хоризонтален слайд (top-level)
- **`--`** - Вертикален подслайд (nested)
- **`Note:`** - Speaker notes (после празен ред)

**Пример:**

```markdown
---

# Главна тема

---

## Детайли

--

### Подробност 1

--

### Подробност 2

---

## Следваща тема

Note:
Това е speaker note за следващата тема.
Вижда се само когато натиснете 'S' за speaker view.
```

### Speaker Notes

Speaker notes са видими само в presenter view (отваря се с клавиш **S**).

```markdown
---

## Важна тема

Текст, който студентите виждат

Note:
Това е забележка само за преподавателя.
- Първа точка за запомняне
- Втора точка за запомняне
- Време: ~5 минути за този слайд
```

**Presenter view показва:**
- Текущ слайд
- Следващ слайд
- Speaker notes
- Таймер
- Прогрес

### Code Highlighting

Reveal.js автоматично хайлайтва код със syntax highlighting:

```markdown
---

## C++ Пример

\`\`\`cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> numbers = {1, 2, 3, 4, 5};

    for (int num : numbers) {
        std::cout << num << " ";
    }

    return 0;
}
\`\`\`

---
```

**Line highlighting** (за подчертаване на определени редове):

```markdown
\`\`\`cpp {2-3}
int main() {
    int x = 10;  // Този ред ще е хайлайтнат
    int y = 20;  // И този
    return 0;
}
\`\`\`
```

### Slide Attributes

Можете да добавяте атрибути към слайдове с HTML коментари:

**Цветен фон:**
```markdown
<!-- .slide: data-background="#4d7e65" -->
## Слайд със зелен фон
```

**Фонова картинка:**
```markdown
<!-- .slide: data-background="path/to/image.jpg" -->
## Слайд с фонова картинка
```

**Transition override:**
```markdown
<!-- .slide: data-transition="zoom" -->
## Този слайд използва zoom transition
```

### Fragments (Incremental Reveals)

Fragments позволяват постепенно показване на съдържание:

```markdown
---

## Поредица от точки

<!-- .element: class="fragment" -->
- Първо се показва тази точка

<!-- .element: class="fragment" -->
- След това тази

<!-- .element: class="fragment" -->
- И накрая тази

---
```

**Fragment ефекти:**
- `fragment` - Просто появяване
- `fragment fade-in` - Fade in
- `fragment fade-out` - Fade out
- `fragment fade-up` - Fade up
- `fragment fade-down` - Fade down
- `fragment highlight-red` - Подчертаване в червено
- `fragment highlight-green` - Подчертаване в зелено
- `fragment highlight-blue` - Подчертаване в синьо

### Keyboard Shortcuts

Студентите могат да използват следните клавишни комбинации:

| Клавиш | Действие |
|--------|----------|
| **Стрелки** | Навигация между слайдове |
| **Space** | Следващ слайд |
| **S** | Отваря speaker view (presenter mode) |
| **F** | Fullscreen mode |
| **ESC** | Overview mode (птичи поглед) |
| **O** | Overview mode (алтернативен начин) |
| **?** | Help overlay със всички shortcuts |
| **B** или **.** | Pause (черен екран) |
| **Home** | Първи слайд |
| **End** | Последен слайд |

### PDF Export

За да експортирате презентация като PDF:

1. **Добавете `?print-pdf`** към URL-а:
   ```
   http://localhost:3000/slides/arrays-binary-search?print-pdf
   ```

2. **Използвайте Chrome браузър**

3. **Print → Save as PDF**:
   - Отворете Print dialog (Ctrl+P / Cmd+P)
   - Destination: Save as PDF
   - Settings: По подразбиране са правилни
   - Save

**Tips за PDF експорт:**
- Уверете се, че всички слайдове са заредени преди печат
- Проверете preview-то преди Save
- Един слайд = една PDF страница

### Adding ViewSlidesButton to Lectures

За да добавите бутон "View Presentation" към lecture.md:

```markdown
---
title: "Лекция"
sidebar_position: 1
slug: lecture
---

import ViewSlidesButton from '@site/src/components/ViewSlidesButton';

# Масиви и Двоично Търсене

<ViewSlidesButton lectureSlug="arrays-binary-search" />

<QuickSummary>
...
</QuickSummary>

[Останала част от лекцията...]
```

**Props:**
- `lectureSlug` (required): Slug на лекцията (напр. "arrays-binary-search")
- `variant` (optional): "primary" или "secondary" (default: "primary")
- `size` (optional): "sm", "md", или "lg" (default: "lg")

### Build Process

Презентациите се генерират автоматично при build:

```bash
cd docusaurus-site
npm run build
```

**Какво се случва:**

1. `reveal-slides-plugin.js` сканира за `docs/*/slides.md` файлове
2. Парсва frontmatter и markdown съдържание
3. Генерира standalone HTML файлове с reveal.js
4. Записва в `build/slides/topic-name/index.html`
5. Презентациите са достъпни на `/slides/topic-name`

**Development mode:**

```bash
npm start
```

Hot reload работи - промените в `slides.md` се отразяват веднага.

### Navigation Integration

Презентациите са интегрирани на три места:

1. **Navbar dropdown** - "Презентации" в главното меню
2. **Slides index page** - `/slides` показва всички презентации
3. **ViewSlidesButton** - Бутон в lecture.md файловете

### Best Practices

#### Content Design

1. **One Idea Per Slide**
   - Всеки слайд трябва да има една основна идея
   - Избягвайте претрупани слайдове

2. **Visual Hierarchy**
   - Използвайте заглавия (H1, H2, H3) за структура
   - Bullet points за списъци
   - Code blocks за примери

3. **Code Examples**
   - Показвайте работещ код
   - Коментирайте важни редове
   - Използвайте line highlighting за фокус

4. **Speaker Notes**
   - Добавяйте notes с допълнителни обяснения
   - Включвайте time estimates
   - Посочвайте често срещани грешки

#### Technical Tips

1. **Test in Browser**
   - Винаги тествайте презентацията в браузър
   - Проверете speaker view ('S' key)
   - Тествайте на мобилни устройства

2. **Keep It Simple**
   - Избягвайте прекалено сложни transitions
   - По-малко fragments = по-добър flow
   - Consistent styling

3. **Accessibility**
   - Използвайте висок contrast
   - Четими font sizes
   - Alt text за images (ако има)

### Troubleshooting

#### Презентацията не се показва

1. **Проверете build output**:
   ```bash
   npm run build
   ls build/slides/your-topic-name/
   ```

2. **Проверете frontmatter**:
   - `title` е задължителен
   - YAML синтаксис е правилен
   - Има `---` в началото и края

3. **Проверете slide delimiters**:
   - `---` с празни редове отгоре и отдолу
   - Не `----` или `--` за horizontal slides

#### Speaker notes не се виждат

- Натиснете **S** key за speaker view
- Уверете се, че има `Note:` след празен ред
- Popup blockers могат да блокират speaker window

#### Code highlighting не работи

- Проверете че езикът е правилен (\`\`\`cpp)
- `highlightTheme: github` в frontmatter
- Reveal.js highlight plugin е enabled (автоматично)

#### PDF export е празен

- Изчакайте всички слайдове да се заредят
- Използвайте Chrome браузър
- Добавете `?print-pdf` ПРЕДИ print

### Example Template

Използвайте този template за нови презентации:

```markdown
---
title: [Заглавие на Лекцията]
theme: white
highlightTheme: github
transition: slide
---

# [Заглавие]

Лекция [N] • Структури от Данни и Програмиране

**ФМИ - Софийски Университет**

Note:
Добре дошли на лекция [N]!

---

## Какво ще научим днес?

- 📦 Първа тема
- 🔍 Втора тема
- ⚡ Трета тема
- 🎯 Четвърта тема

Note:
[Обяснения за преподавателя]

---

## [Основна Тема 1]

**Дефиниция**: [...]

**Характеристики:**
- ✅ Предимство 1
- ✅ Предимство 2
- ❌ Недостатък 1

Note:
[Допълнителни бележки]

---

## Код Пример

\`\`\`cpp
#include <iostream>

int main() {
    // Ваш код тук
    return 0;
}
\`\`\`

Note:
[Обяснение на кода]

---

## Ключови изводи

- 🎯 Извод 1
- 📦 Извод 2
- 🔍 Извод 3

---

## Въпроси?

📧 Email: your-email@fmi.uni-sofia.bg

📖 Материали: [GitHub](https://github.com/alexaytov/fmi-sdp-exc-2025-26)

---

## Благодаря за вниманието!

**Shortcuts:** S (speaker), F (fullscreen), ? (help)

📊 [Всички презентации](/slides)

Note:
Успех с упражненията!
```

### Advanced Features

#### Vertical Slides

Използвайте вертикални слайдове за детайли:

```markdown
---

# Главна тема

---

## Детайли

Навигирайте надолу (↓) за повече информация

--

### Подробност 1

Това е вертикален подслайд

--

### Подробност 2

Още един вертикален подслайд

---

# Следваща главна тема
```

**Навигация:**
- → и ← за хоризонтални слайдове
- ↓ и ↑ за вертикални слайдове

#### Two-Column Layout

За two-column layout използвайте HTML:

```markdown
---

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">

<div>

## Лява колона

- Точка 1
- Точка 2

</div>

<div>

## Дясна колона

- Точка А
- Точка Б

</div>

</div>

---
```

#### Math Equations

Reveal.js поддържа LaTeX математика (ако е configured):

```markdown
---

## Времева сложност

Двоично търсене: $O(\log n)$

$$
T(n) = T\left(\frac{n}{2}\right) + O(1)
$$

---
```

**Note:** Math support може да изисква допълнителна конфигурация в plugin-а.

---

**Последна актуализация**: 9 Декември 2025

## Changelog

### 10 Декември 2025
- **Reveal.js Final Implementation**: Финална работеща имплементация с pathname:// protocol
- **Static Files Approach**: Презентациите се генерират като статични HTML файлове в static/slides/
- **Navigation Fix**: Използва `pathname://` в navbar и абсолютен URL в React компоненти
- **Bulgarian UI**: Всички бутони и labels преведени на български ("Виж Презентация")
- **New Tab**: Презентациите се отварят в нов таб с `target="_blank"`
- **No SPA Conflicts**: Заобикаля Docusaurus SPA routing без React hydration errors

### 9 Декември 2025
- **Reveal.js Integration**: Добавена пълна поддръжка за интерактивни презентации
- **Auto-generation**: Презентациите се генерират автоматично от slides.md файлове
- **Navigation**: Интегрирани бутони и navbar dropdown за достъп до презентации
- **Speaker Notes**: Пълна поддръжка за presenter view с notes и timer
- **PDF Export**: Built-in поддръжка за експорт като PDF
- **Fix**: slides.md файловете се exclude-ват от docs plugin (не създават doc pages)

### 2 Декември 2025
- **React Context Architecture**: Пълна миграция към Context-based решение
- **Zero Config Approach**: Премахнато нуждата от `exercises` prop и ръчни IDs
- **Infinite Loop Prevention**: Документирани критични patterns за useEffect dependencies
- **Auto-ID Generation**: Semantic IDs базирани на DOM traversal на заглавия
- **MDX Theme Swizzling**: ExerciseProvider wrapper за всички MDX страници

### 28 Ноември 2025
- Първоначална документация за auto-generated IDs
- Миграционни скриптове за semantic IDs
