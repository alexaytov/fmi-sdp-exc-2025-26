// Solutions for Tree and BST Exercises
window.solutions = {
    ex1: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>Дърво</strong> е йерархична структура от данни, състояща се от възли, свързани чрез ребра, където:</p>
        <ul>
            <li>Има точно един <strong>корен</strong> възел (без родител)</li>
            <li>Всеки възел (освен корена) има точно един родител</li>
            <li>Няма цикли - съществува единствен път между всеки два възела</li>
            <li>Всеки възел може да има нула или повече деца</li>
        </ul>
        <p><strong>Защо са нелинейни?</strong></p>
        <ul>
            <li>В линейни структури (масиви, свързани списъци) елементите се обхождат в една последователност</li>
            <li>В дървета един възел може да има множество деца, създавайки различни пътища за обхождане</li>
            <li>Данните са организирани йерархично, не в линейна последователност</li>
        </ul>
    </div>`,

    ex2: `<div class="solution-content">
        <h4>Решение:</h4>
        <p>Правилни отговори: <strong>b), c), и d)</strong></p>
        <ul>
            <li><strong>a) Невярно</strong> - 30 е <strong>дете</strong> на 50, не родител</li>
            <li><strong>b) Вярно</strong> - 30 и 70 имат общ родител (50), следователно са братя и сестри</li>
            <li><strong>c) Вярно</strong> - В дадения контекст 50 няма родител, следователно е коренен възел</li>
            <li><strong>d) Вярно</strong> - Възел без деца по дефиниция е лист</li>
        </ul>
    </div>`,

    ex3: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">struct TreeNode {
    int data;           // Съхранява целочислена стойност
    TreeNode* left;     // Указател към ляво дете
    TreeNode* right;    // Указател към дясно дете
};</code></pre>
        </div>
        <p><strong>Обяснение:</strong></p>
        <ul>
            <li><code>data</code> съхранява стойността на възела</li>
            <li><code>left</code> и <code>right</code> са указатели към децата</li>
            <li>Използват се указатели, за да се позволи динамично създаване на възли</li>
        </ul>
    </div>`,

    ex4: `<div class="solution-content">
        <h4>Решение:</h4>
        <p>За балансирано BST с n елемента, височината h ≈ log₂(n)</p>
        <p>За <strong>1,000 елемента</strong>:</p>
        <ul>
            <li>h = ⌈log₂(1000)⌉ = ⌈9.97⌉ = <strong>10</strong></li>
            <li>В най-лошия случай трябва да проверим всички нива от корена до листата</li>
            <li>Следователно: <strong>10 сравнения</strong></li>
        </ul>
        <div class="code-block">
            <pre><code class="language-plaintext">Изчисление:
2^9 = 512 < 1000
2^10 = 1024 > 1000
Следователно: височина = 10

Най-лош случай: започваме от корен, отиваме до най-дълбокия лист
Брой сравнения = височина = 10</code></pre>
        </div>
        <p><strong>Обща формула:</strong> За n елемента в балансирано дърво, максимум <code>⌈log₂(n)⌉</code> сравнения</p>
    </div>`,

    ex5: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-plaintext">Последователност: 50, 30, 70, 20, 40

Стъпка по стъпка:
1. Вмъкни 50:        50

2. Вмъкни 30:        50
                    /
                   30

3. Вмъкни 70:        50
                    /  \\
                   30   70

4. Вмъкни 20:        50
                    /  \\
                   30   70
                  /
                 20

5. Вмъкни 40:        50
                    /  \\
                   30   70
                  /  \\
                 20   40

Финално дърво:
        50
       /  \\
      30   70
     /  \\
    20   40</code></pre>
        </div>
    </div>`,

    ex6: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">// Проверка дали дървото е празно
if (root == nullptr) {
    return;  // или true/false в зависимост от функцията
}

// Алтернативно (по-кратко):
if (!root) {
    return;
}</code></pre>
        </div>
        <p><strong>Обяснение:</strong></p>
        <ul>
            <li>Базовият случай проверява дали указателят е NULL/nullptr</li>
            <li>Това спира рекурсията и предотвратява segmentation fault</li>
            <li>Всяка рекурсивна функция за дървета започва с тази проверка</li>
        </ul>
    </div>`,

    ex7: `<div class="solution-content">
        <h4>Решение:</h4>
        <ul>
            <li><strong>Inorder:</strong> (Ляво, Корен, Дясно)</li>
            <li><strong>Preorder:</strong> (Корен, Ляво, Дясно)</li>
            <li><strong>Postorder:</strong> (Ляво, Дясно, Корен)</li>
        </ul>
        <p><strong>Мнемоника:</strong> Позицията на "Корен" в името показва кога се посещава:</p>
        <ul>
            <li><strong>In</strong>order - Корен е <strong>в средата</strong></li>
            <li><strong>Pre</strong>order - Корен е <strong>преди</strong> децата</li>
            <li><strong>Post</strong>order - Корен е <strong>след</strong> децата</li>
        </ul>
    </div>`,

    ex8: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>Вярно!</strong> Това е основното <strong>BST инвариант</strong>.</p>
        <p><strong>Защо е важно?</strong></p>
        <ul>
            <li><strong>Ефективно търсене:</strong> Позволява елиминиране на половината дърво при всяко сравнение</li>
            <li><strong>O(log n) производителност:</strong> В балансирани дървета това свойство дава логаритмична сложност</li>
            <li><strong>Сортирани данни:</strong> Inorder обхождане автоматично произвежда сортирани елементи</li>
            <li><strong>Бърза проверка за диапазони:</strong> Лесно намиране на всички елементи между две стойности</li>
        </ul>
        <p><strong>Важно:</strong> Свойството важи за <strong>всички наследници</strong>, не само непосредствените деца!</p>
    </div>`,

    ex9: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">int countNodes(TreeNode* root) {
    // Базов случай: празно дърво има 0 възли
    if (root == nullptr) {
        return 0;
    }
    
    // Рекурсивен случай: 
    // 1 (текущ възел) + възли в ляво + възли в дясно
    return 1 + countNodes(root->left) + countNodes(root->right);
}</code></pre>
        </div>
        <p><strong>Обяснение:</strong></p>
        <ul>
            <li>Базовият случай: празното дърво няма възли (0)</li>
            <li>Рекурсивно броим възлите в лявото и дясното поддърво</li>
            <li>Добавяме 1 за текущия възел</li>
            <li><strong>Времева сложност:</strong> O(n) - посещаваме всеки възел веднъж</li>
        </ul>
    </div>`,

    ex10: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>Защо inorder дава сортирани стойности в BST?</strong></p>
        <p>Поради BST свойството и реда на посещение:</p>
        <ol>
            <li>Първо посещаваме лявото поддърво (всички стойности &lt; корен)</li>
            <li>После посещаваме корена</li>
            <li>Накрая посещаваме дясното поддърво (всички стойности &gt; корен)</li>
        </ol>
        <p><strong>Пример с 3-възлово дърво:</strong></p>
        <div class="code-block">
            <pre><code class="language-plaintext">    50
   /  \\
  30   70

Inorder: посети(30) → посети(50) → посети(70)
Изход: 30, 50, 70 (сортирано възходящо!)</code></pre>
        </div>
        <p>Това свойство прави BST отличен за динамично сортиране!</p>
    </div>`,

    ex11: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">bool search(Node* root, int key) {
    // Базов случай: достигнали сме края, не е намерен
    if (root == nullptr) {
        return false;
    }
    
    // Намерен!
    if (root->data == key) {
        return true;
    }
    
    // Рекурсивно търси в подходящото поддърво
    if (key < root->data) {
        return search(root->left, key);  // Търси в лявото
    } else {
        return search(root->right, key); // Търси в дясното
    }
}

// Итеративна версия (по-ефективна за памет):
bool searchIterative(Node* root, int key) {
    while (root != nullptr) {
        if (root->data == key) return true;
        else if (key < root->data) root = root->left;
        else root = root->right;
    }
    return false;
}</code></pre>
        </div>
        <p><strong>Сложност:</strong></p>
        <ul>
            <li>Best case: O(1) - елементът е в корена</li>
            <li>Average case: O(log n) - балансирано дърво</li>
            <li>Worst case: O(n) - изродено дърво (свързан списък)</li>
        </ul>
    </div>`,

    ex12: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>Минимален брой възли:</strong> h + 1</p>
        <ul>
            <li>Възниква когато дървото е "слабо" (всеки възел има само едно дете)</li>
            <li>Примерно дърво с височина 3: 4 възела в права линия</li>
        </ul>
        <p><strong>Максимален брой възли:</strong> 2^(h+1) - 1</p>
        <ul>
            <li>Възниква когато дървото е пълно (всеки възел има две деца)</li>
            <li>Примерно дърво с височина 3: 15 възела (1+2+4+8)</li>
        </ul>
        <div class="code-block">
            <pre><code class="language-plaintext">h = 0: min = 1,  max = 1    (само корен)
h = 1: min = 2,  max = 3    (корен + 1-2 деца)
h = 2: min = 3,  max = 7    (до 3 нива)
h = 3: min = 4,  max = 15   (до 4 нива)

Общо: min = h + 1,  max = 2^(h+1) - 1</code></pre>
        </div>
    </div>`,

    ex13: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>1. Финална структура:</strong></p>
        <div class="code-block">
            <pre><code class="language-plaintext">        10
       /  \\
      5    15
     / \\  /  \\
    3   7 12  20</code></pre>
        </div>
        <p><strong>2. Inorder обхождане:</strong> 3, 5, 7, 10, 12, 15, 20 (възходящ ред!)</p>
        <p><strong>3. Preorder обхождане:</strong> 10, 5, 3, 7, 15, 12, 20</p>
        <p><strong>Стъпки на построяване:</strong></p>
        <ol>
            <li>10 става корен</li>
            <li>5 &lt; 10 → отива наляво</li>
            <li>15 &gt; 10 → отива надясно</li>
            <li>3 &lt; 10 и 3 &lt; 5 → отива наляво на 5</li>
            <li>7 &lt; 10 но 7 &gt; 5 → отива надясно на 5</li>
            <li>12 &gt; 10 но 12 &lt; 15 → отива наляво на 15</li>
            <li>20 &gt; 10 и 20 &gt; 15 → отива надясно на 15</li>
        </ol>
    </div>`,

    ex14: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>Дърво A:</strong> ✅ <strong>Валидно BST</strong></p>
        <ul>
            <li>За 10: ляво (5, 3, 7) &lt; 10 &lt; дясно (15)</li>
            <li>За 5: ляво (3) &lt; 5 &lt; дясно (7)</li>
            <li>Всички проверки минават</li>
        </ul>
        <p><strong>Дърво B:</strong> ✅ <strong>Валидно BST</strong></p>
        <ul>
            <li>За 10: ляво (5, 2, 6) &lt; 10 &lt; дясно (15, 12, 20)</li>
            <li>За 5: ляво (2) &lt; 5 &lt; дясно (6)</li>
            <li>За 15: ляво (12) &lt; 15 &lt; дясно (20)</li>
        </ul>
        <p><strong>Дърво C:</strong> ❌ <strong>Невалидно BST</strong></p>
        <ul>
            <li><strong>Проблем:</strong> 12 е в <strong>лявото поддърво</strong> на корена 10</li>
            <li>Но 12 &gt; 10, следователно трябва да е в дясното поддърво!</li>
            <li>Нарушава основното BST свойство</li>
        </ul>
        <p><strong>Важен урок:</strong> Трябва да проверим всички наследници, не само непосредствените деца!</p>
    </div>`,

    ex15: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">Node* insert(Node* root, int key) {
    if (!root) return new Node(key);
    
    if (key < root->data)              // Първо условие
        root->left = insert(root->left, key);
    else if (key > root->data)         // Второ условие
        root->right = insert(root->right, key);
    
    return root;
}</code></pre>
        </div>
        <p><strong>Обяснение на условията:</strong></p>
        <ul>
            <li><strong>Първо:</strong> <code>key &lt; root->data</code> - ключът е по-малък, отиваме наляво</li>
            <li><strong>Второ:</strong> <code>key &gt; root->data</code> - ключът е по-голям, отиваме надясно</li>
            <li>Ако key == root->data, не правим нищо (дублирани ключове обикновено не се добавят)</li>
        </ul>
    </div>`,

    ex16: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>Защо се дегенерира?</strong></p>
        <div class="code-block">
            <pre><code class="language-plaintext">Вмъкване в ред: 1, 2, 3, 4, 5

Стъпка 1: 1
Стъпка 2: 1 (всичко > 1 отива надясно)
            \\
             2
Стъпка 3: 1
            \\
             2
              \\
               3
...
Финално:  1
            \\
             2
              \\
               3
                \\
                 4
                  \\
                   5
                   
Резултат: СВЪРЗАН СПИСЪК!</code></pre>
        </div>
        <p><strong>Времева сложност на търсене:</strong> O(n)</p>
        <ul>
            <li>В най-лошия случай трябва да проверим всички възли</li>
            <li>Дървото има височина n-1 вместо log n</li>
            <li>Губим всички предимства на BST структурата</li>
        </ul>
        <p><strong>Решение:</strong> Използвайте самобалансиращи се дървета (AVL, Red-Black)</p>
    </div>`,

    ex17: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">Node* minValue(Node* node) {
    // Най-малката стойност винаги е най-вляво
    while (node != nullptr && node->left != nullptr) {
        node = node->left;
    }
    return node;
}

// Алтернативно (рекурсивно):
Node* minValueRecursive(Node* node) {
    if (node == nullptr || node->left == nullptr) {
        return node;
    }
    return minValueRecursive(node->left);
}</code></pre>
        </div>
        <p><strong>Защо работи?</strong></p>
        <ul>
            <li>По BST свойството: <strong>всички стойности наляво &lt; текуща стойност</strong></li>
            <li>Следователно най-малката стойност е в най-левия лист</li>
            <li>Просто следваме левите указатели докато можем</li>
        </ul>
        <p><strong>Забележка:</strong> За максимум, следваме десните указатели!</p>
    </div>`,

    ex18: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>Начална структура:</strong></p>
        <div class="code-block">
            <pre><code class="language-plaintext">        15
       /  \\
      10   20
     / \\  / \\
    8  12 17 25</code></pre>
        </div>
        <p><strong>Изтриване на 10 (има две деца: 8 и 12):</strong></p>
        <ol>
            <li>Намери <strong>inorder последовател</strong> на 10</li>
            <li>Inorder последовател = най-малката стойност в дясното поддърво</li>
            <li>За 10: дясното поддърво е {12}, минимум е <strong>12</strong></li>
            <li>Замени 10 със 12</li>
            <li>Изтрий оригиналния възел 12 (той има 0 или 1 дете)</li>
        </ol>
        <p><strong>Финална структура:</strong></p>
        <div class="code-block">
            <pre><code class="language-plaintext">        15
       /  \\
      12   20
     /    / \\
    8    17 25</code></pre>
        </div>
        <p><strong>Защо 12?</strong> Inorder последовател запазва BST свойството, защото той е най-малкият елемент, по-голям от изтриваната стойност!</p>
    </div>`,

    ex19: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>1. Изчисляване на височина:</strong></p>
        <div class="code-block">
            <pre><code class="language-plaintext">        50 (h=4)
       /  \\
      30   70
     /    /  \\
   20   60   80
   /         \\
  10         90

Височини по възли:
- 10: h=0 (лист)
- 20: h=1 (1 + max(h(10), 0))
- 30: h=2 (1 + max(h(20), 0))
- 60: h=0 (лист)
- 90: h=0 (лист)
- 80: h=1 (1 + max(0, h(90)))
- 70: h=2 (1 + max(h(60), h(80)))
- 50: h=3 (1 + max(h(30), h(70)))</code></pre>
        </div>
        <p><strong>Височина на дървото: 3</strong> (броим ребрата от корен до най-далечен лист: 50→30→20→10)</p>
        <p><strong>2. Проверка за баланс:</strong></p>
        <ul>
            <li>За 50: |h(30) - h(70)| = |2 - 2| = 0 ≤ 1 ✅</li>
            <li>За 30: |h(20) - 0| = 1 ≤ 1 ✅</li>
            <li>За 70: |h(60) - h(80)| = |0 - 1| = 1 ≤ 1 ✅</li>
            <li>За 20: |h(10) - 0| = 0 ≤ 1 ✅</li>
            <li>За 80: |0 - h(90)| = 0 ≤ 1 ✅</li>
        </ul>
        <p><strong>Заключение: Дървото е БАЛАНСИРАНО</strong> ✅</p>
    </div>`,

    ex20: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">int findMax(Node* root) {
    // Проверка за празно дърво
    if (root == nullptr) {
        throw std::runtime_error("Празно дърво няма максимум!");
    }
    
    // Най-голямата стойност е най-вдясно
    while (root->right != nullptr) {
        root = root->right;
    }
    
    return root->data;
}</code></pre>
        </div>
        <p><strong>Обяснение:</strong></p>
        <ul>
            <li>Обратно на минимум - следваме <strong>десните</strong> указатели</li>
            <li>По BST свойството: всички стойности надясно &gt; текущата</li>
            <li>Следователно най-дясният възел има максималната стойност</li>
            <li><strong>Итеративен:</strong> Използва O(1) допълнителна памет (без stack от рекурсията)</li>
        </ul>
        <p><strong>Сложност:</strong> O(h) където h е височината на дървото</p>
    </div>`,

    ex21: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">Node* deleteNode(Node* root, int key) {
    // Базов случай: празно дърво
    if (root == nullptr) {
        return root;
    }
    
    // Навигация до възела за изтриване
    if (key < root->data) {
        root->left = deleteNode(root->left, key);
    } 
    else if (key > root->data) {
        root->right = deleteNode(root->right, key);
    } 
    else {
        // Възелът е намерен! Обработка на трите случая
        
        // СЛУЧАЙ 1: Няма ляво дете (включва лист)
        if (root->left == nullptr) {
            Node* temp = root->right;
            delete root;
            return temp;
        }
        
        // СЛУЧАЙ 2: Няма дясно дете
        if (root->right == nullptr) {
            Node* temp = root->left;
            delete root;
            return temp;
        }
        
        // СЛУЧАЙ 3: Има две деца
        // Намери inorder последовател (минимум в дясно)
        Node* temp = minValue(root->right);
        
        // Копирай стойността на последователя в този възел
        root->data = temp->data;
        
        // Изтрий последователя (има най-много 1 дете)
        root->right = deleteNode(root->right, temp->data);
    }
    
    return root;
}

// Помощна функция
Node* minValue(Node* node) {
    while (node->left != nullptr) {
        node = node->left;
    }
    return node;
}</code></pre>
        </div>
        <p><strong>Ключови точки:</strong></p>
        <ul>
            <li>Случаи 1 и 2 са прости - само пренасочваме указатели</li>
            <li>Случай 3 е сложен - използваме inorder последовател</li>
            <li><strong>Защо inorder последовател?</strong> Той е най-малкият в дясното поддърво, гарантира запазване на BST свойството</li>
        </ul>
    </div>`,

    ex22: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">// Помощна функция с мин/макс граници
bool isValidBSTHelper(TreeNode* root, long long minVal, long long maxVal) {
    // Базов случай: празно поддърво е валидно
    if (root == nullptr) {
        return true;
    }
    
    // Проверка на текущия възел
    if (root->data <= minVal || root->data >= maxVal) {
        return false;
    }
    
    // Рекурсивно проверяваме поддърветата с актуализирани граници
    return isValidBSTHelper(root->left, minVal, root->data) &&
           isValidBSTHelper(root->right, root->data, maxVal);
}

bool isValidBST(TreeNode* root) {
    // Започваме с неограничени граници
    return isValidBSTHelper(root, LLONG_MIN, LLONG_MAX);
}</code></pre>
        </div>
        <p><strong>Обяснение:</strong></p>
        <ul>
            <li><strong>minVal/maxVal</strong> представляват допустимия диапазон за текущия възел</li>
            <li>За <strong>ляво поддърво</strong>: maxVal става root->data (всички &lt; root)</li>
            <li>За <strong>дясно поддърво</strong>: minVal става root->data (всички &gt; root)</li>
            <li>Използваме <code>long long</code> за да избегнем integer overflow</li>
        </ul>
        <p><strong>Защо проверката само на деца НЕ работи?</strong></p>
        <div class="code-block">
            <pre><code class="language-plaintext">    10
   /  \\
  5   15
       /
      12  ← 12 &lt; 15 ✓, но 12 &gt; 10 ✗ (нарушава BST!)</code></pre>
        </div>
    </div>`,

    ex23: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>1. Достъп до дясното дете:</strong></p>
        <div class="code-block">
            <pre><code class="language-cpp">int rightChild(int arr[], int size, int i) {
    int rightIndex = 2 * i + 2;  // Формула за дясно дете
    
    if (rightIndex >= size) {
        return -1;  // Няма дясно дете
    }
    
    return arr[rightIndex];
}

// Пример: arr = [10, 5, 15, 3, 7, 12, 20]
// rightChild(arr, 7, 0) → индекс 2 → стойност 15
// rightChild(arr, 7, 1) → индекс 4 → стойност 7</code></pre>
        </div>
        <p><strong>2. Конверсия към свързана структура:</strong></p>
        <div class="code-block">
            <pre><code class="language-cpp">Node* arrayToTree(int arr[], int size, int i = 0) {
    // Базов случай: индекс извън границите
    if (i >= size) {
        return nullptr;
    }
    
    // Създай възел за текущия индекс
    Node* node = new Node(arr[i]);
    
    // Рекурсивно създай деца
    node->left = arrayToTree(arr, size, 2*i + 1);   // Ляво
    node->right = arrayToTree(arr, size, 2*i + 2);  // Дясно
    
    return node;
}</code></pre>
        </div>
        <p><strong>3. Сравнение на ефективност:</strong></p>
        <table>
            <tr>
                <th>Критерий</th>
                <th>Масив</th>
                <th>Свързана Структура</th>
            </tr>
            <tr>
                <td>Памет за n възли</td>
                <td>n × sizeof(int)</td>
                <td>n × (sizeof(int) + 2×sizeof(Node*))</td>
            </tr>
            <tr>
                <td>Допълнителна памет</td>
                <td>Може да има празни слотове</td>
                <td>Само използвани възли</td>
            </tr>
            <tr>
                <td>За пълно дърво</td>
                <td>✅ По-ефективно</td>
                <td>❌ Повече памет</td>
            </tr>
            <tr>
                <td>За редко дърво</td>
                <td>❌ Губи място</td>
                <td>✅ По-ефективно</td>
            </tr>
        </table>
    </div>`,

    ex24: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>Случаен ред на елементите:</strong></p>
        <ul>
            <li>Всяко вмъкване: O(log n) среден случай</li>
            <li>За n елемента: n × O(log n) = <strong>O(n log n)</strong></li>
            <li>Дървото е приблизително балансирано</li>
        </ul>
        <p><strong>Сортиран ред на елементите:</strong></p>
        <ul>
            <li>Дървото дегенерира в свързан списък</li>
            <li>Първо вмъкване: O(1), второ: O(2), трето: O(3), ...</li>
            <li>n-то вмъкване: O(n)</li>
            <li>Общо: 1 + 2 + 3 + ... + n = n(n+1)/2 = <strong>O(n²)</strong></li>
        </ul>
        <div class="code-block">
            <pre><code class="language-plaintext">Производителност за 1000 елемента:

Случаен ред:  ~10,000 операции    (1000 × log₂1000 ≈ 10,000)
Сортиран ред: ~500,000 операции   (1000 × 1000 / 2)

Разлика: 50x по-бавно!</code></pre>
        </div>
        <p><strong>Защо е важно?</strong></p>
        <ul>
            <li>Реалните данни често пристигат в почти сортиран ред</li>
            <li>Без балансиране, BST може да бъде много бавен</li>
            <li>Затова продукционните системи използват AVL/Red-Black дървета</li>
        </ul>
    </div>`,

    ex25: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">// Решение с помощна функция и брояч
void kthSmallestHelper(Node* root, int& k, int& result) {
    if (root == nullptr || k <= 0) {
        return;
    }
    
    // Inorder: ляво, корен, дясно
    kthSmallestHelper(root->left, k, result);
    
    // Намалявай k и провери дали сме стигнали k-тия
    k--;
    if (k == 0) {
        result = root->data;
        return;
    }
    
    kthSmallestHelper(root->right, k, result);
}

int kthSmallest(Node* root, int k) {
    int result = -1;
    kthSmallestHelper(root, k, result);
    return result;
}

// Пример:
// BST: 10, 5, 15, 3, 7, 12, 20
// Inorder: 3, 5, 7, 10, 12, 15, 20
// kthSmallest(root, 1) → 3
// kthSmallest(root, 4) → 10
// kthSmallest(root, 7) → 20</code></pre>
        </div>
        <p><strong>Сложност:</strong></p>
        <ul>
            <li><strong>Време:</strong> O(k) - спираме след k възли</li>
            <li><strong>Памет:</strong> O(h) - stack за рекурсия</li>
            <li>По-ефективно от O(n) за малки k</li>
        </ul>
    </div>`,

    ex26: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">Node* lowestCommonAncestor(Node* root, int n1, int n2) {
    // Базов случай
    if (root == nullptr) {
        return nullptr;
    }
    
    // Ако двете стойности са по-малки от корена,
    // LCA е в лявото поддърво
    if (n1 < root->data && n2 < root->data) {
        return lowestCommonAncestor(root->left, n1, n2);
    }
    
    // Ако двете стойности са по-големи от корена,
    // LCA е в дясното поддърво
    if (n1 > root->data && n2 > root->data) {
        return lowestCommonAncestor(root->right, n1, n2);
    }
    
    // Ако стойностите са от различни страни
    // (или една от тях е коренът), текущият възел е LCA
    return root;
}

// Пример:
//         20
//        /  \\
//      10    30
//     /  \\
//    5   15
//
// LCA(5, 15) = 10
// LCA(5, 30) = 20
// LCA(10, 15) = 10 (един от възлите е LCA)</code></pre>
        </div>
        <p><strong>Обяснение:</strong></p>
        <ul>
            <li>Използваме BST свойството за ефективна навигация</li>
            <li>LCA е точката където пътищата се разделят</li>
            <li>Ако едната стойност е наляво, а другата надясно - текущият възел е LCA</li>
            <li><strong>Сложност:</strong> O(h) = O(log n) за балансирано дърво</li>
        </ul>
    </div>`,

    ex27: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">#include &lt;iostream&gt;
#include &lt;string&gt;
using namespace std;

struct Contact {
    string name;
    string phone;
    Contact* left;
    Contact* right;
    
    Contact(string n, string p) 
        : name(n), phone(p), left(nullptr), right(nullptr) {}
};

class PhoneBook {
private:
    Contact* root;
    
    Contact* insertHelper(Contact* node, string name, string phone) {
        if (node == nullptr) {
            return new Contact(name, phone);
        }
        if (name < node->name) {
            node->left = insertHelper(node->left, name, phone);
        } else if (name > node->name) {
            node->right = insertHelper(node->right, name, phone);
        } else {
            node->phone = phone;  // Актуализиране
        }
        return node;
    }
    
    Contact* searchHelper(Contact* node, string name) {
        if (node == nullptr) return nullptr;
        if (name == node->name) return node;
        else if (name < node->name) 
            return searchHelper(node->left, name);
        else 
            return searchHelper(node->right, name);
    }
    
    void printHelper(Contact* node) {
        if (node == nullptr) return;
        printHelper(node->left);
        cout << node->name << ": " << node->phone << endl;
        printHelper(node->right);
    }
    
public:
    PhoneBook() : root(nullptr) {}
    
    void insert(string name, string phone) {
        root = insertHelper(root, name, phone);
    }
    
    string search(string name) {
        Contact* contact = searchHelper(root, name);
        return contact ? contact->phone : "Не е намерен";
    }
    
    void printAll() {
        cout << "=== Телефонен Указател ===" << endl;
        printHelper(root);  // Отпечатва в азбучен ред!
    }
};

// Използване:
int main() {
    PhoneBook book;
    book.insert("Иван", "0888123456");
    book.insert("Мария", "0877654321");
    book.insert("Георги", "0899111222");
    
    cout << book.search("Мария") << endl;
    book.printAll();
    return 0;
}</code></pre>
        </div>
        <p><strong>Предимства:</strong> O(log n) операции, азбучен ред автоматично</p>
    </div>`,

    ex28: `<div class="solution-content">
        <h4>Решение:</h4>
        <table>
            <tr>
                <th>Операция</th>
                <th>Сортиран Масив</th>
                <th>Балансирано BST</th>
                <th>Победител</th>
            </tr>
            <tr>
                <td><strong>Търсене</strong></td>
                <td>O(log n)</td>
                <td>O(log n)</td>
                <td>🤝 Равни</td>
            </tr>
            <tr>
                <td><strong>Вмъкване</strong></td>
                <td>O(n)</td>
                <td>O(log n)</td>
                <td>🌳 BST</td>
            </tr>
            <tr>
                <td><strong>Изтриване</strong></td>
                <td>O(n)</td>
                <td>O(log n)</td>
                <td>🌳 BST</td>
            </tr>
        </table>
        <p><strong>Избор на структура:</strong></p>
        <ul>
            <li><strong>Масив:</strong> Рядко променящи се данни, важна cache locality</li>
            <li><strong>BST:</strong> Чести вмъквания/изтривания, динамичен размер</li>
        </ul>
    </div>`,

    ex29: `<div class="solution-content">
        <h4>Решение:</h4>
        <p><strong>Анализ:</strong> Стандартен BST <strong>НЕ е оптимален</strong> за тази задача!</p>
        <p><strong>Проблеми със стандартен BST:</strong></p>
        <ul>
            <li><strong>Медиана:</strong> Намирането на медиана изисква обхождане → O(n)</li>
            <li><strong>Дегенерация:</strong> Без балансиране може да стане O(n) за операции</li>
        </ul>
        <p><strong>По-добра алтернатива: Две Heap структури</strong></p>
        <div class="code-block">
            <pre><code class="language-cpp">// Max heap за по-малката половина
priority_queue&lt;int&gt; maxHeap;

// Min heap за по-голямата половина  
priority_queue&lt;int, vector&lt;int&gt;, greater&lt;int&gt;&gt; minHeap;

// Медианата е винаги на върха на един от heap-овете!
// Време: O(1) за намиране, O(log n) за вмъкване</code></pre>
        </div>
        <p><strong>Заключение:</strong> За тази задача използвай два heap-а вместо BST!</p>
    </div>`,

    ex30: `<div class="solution-content">
        <h4>Решение:</h4>
        <div class="code-block">
            <pre><code class="language-cpp">void treeSort(int arr[], int n) {
    // Стъпка 1: Изгради BST
    Node* root = nullptr;
    for (int i = 0; i < n; i++) {
        root = insert(root, arr[i]);
    }
    
    // Стъпка 2: Inorder обхождане → сортирани данни
    int index = 0;
    storeInorder(root, arr, index);
}

void storeInorder(Node* root, int arr[], int& index) {
    if (root == nullptr) return;
    
    storeInorder(root->left, arr, index);
    arr[index++] = root->data;
    storeInorder(root->right, arr, index);
}</code></pre>
        </div>
        <p><strong>Анализ на сложността:</strong></p>
        <ul>
            <li><strong>Най-добър случай: O(n log n)</strong> - балансирано дърво</li>
            <li><strong>Най-лош случай: O(n²)</strong> - сортирани данни → дегенерация</li>
        </ul>
        <div class="code-block">
            <pre><code class="language-plaintext">Сравнение с други алгоритми:
- Quick Sort: O(n log n) среден, O(n²) най-лош
- Merge Sort: O(n log n) винаги гарантирано
- Tree Sort: O(n log n) среден, O(n²) най-лош

Tree Sort е полезен когато:
- Данните пристигат постепенно
- Нужни са допълнителни заявки (търсене, диапазон)</code></pre>
        </div>
    </div>`
};
